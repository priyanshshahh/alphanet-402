{
  "name": "alphanet-402",
  "private": true,
  "description": "Root scripts delegate to alphanet-core/frontend — run from repo root.",
  "scripts": {
    "record:demo:install": "npm run record:demo:install --prefix alphanet-core/frontend",
    "record:demo": "npm run record:demo --prefix alphanet-core/frontend",
    "dev:frontend": "npm run dev --prefix alphanet-core/frontend",
    "build:frontend": "npm run build --prefix alphanet-core/frontend"
  }
}
